import React from "react";
import RegisterPage from "../auth/RegisterPage";

const HomePage = () => {
    return (
        <div>
            <RegisterPage />
        </div>
    );
};

export default HomePage;
